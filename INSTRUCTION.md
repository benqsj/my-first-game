# ინსტრუქცია — Vepxis

როგორ გავუშვათ თამაში, რა ღილაკები მართავს რას და როგორ შევამოწმოთ, რომ
ყველაფერი მუშაობს.

---

## 1. რა გჭირდება

| საჭირო | ვერსია | შემოწმება |
| ------ | ------ | --------- |
| Godot Engine | **4.7** ან უფრო ახალი (Forward+ რენდერერი) | `godot --version` |

თუ `godot` ბრძანება ვერ მოიძებნა, ან [godotengine.org](https://godotengine.org/download)-დან
ჩამოწერე `Godot.app`, ან Homebrew-ით:

```bash
brew install --cask godot
# და შემდეგ, რომ ტერმინალიდანაც იმუშაოს:
sudo ln -s /Applications/Godot.app/Contents/MacOS/Godot /opt/homebrew/bin/godot
```

---

## 2. თამაშის გაშვება

პროექტის საქაღალდიდან (`/Users/macbook/Desktop/vepxis`):

```bash
godot --path .
```

ეს პირდაპირ **თამაშს უშვებს** — მთავარი სცენაა
`res://scenes/world/greybox_world.tscn` (`project.godot`-ში მითითებული).

სხვა სასარგებლო ვარიანტები:

```bash
godot -e --path .                       # რედაქტორის გახსნა (თამაშის ნაცვლად)
godot --path . --resolution 2560x1440   # კონკრეტული გარჩევადობით
godot --path . --scene res://scenes/world/greybox_world.tscn   # კონკრეტული სცენა
godot --path . --fullscreen             # პირდაპირ სრულ ეკრანზე
```

საქაღალდის გარედან საკმარისია სრული გზა მისცე:

```bash
godot --path /Users/macbook/Desktop/vepxis
```

გამოსვლა: **Escape** ათავისუფლებს მაუსს, შემდეგ ფანჯრის დახურვა (⌘Q).

---

## 3. მართვა

| მოქმედება | კლავიატურა / მაუსი | გეიმპადი |
| --------- | ------------------ | -------- |
| სიარული წინ / უკან / გვერდებზე | `W` `A` `S` `D` | მარცხენა სტიკი |
| კამერა | მაუსის მოძრაობა | მარჯვენა სტიკი |
| ხტომა | `Space` | A / Cross |
| გადაბრუნება (dodge roll) | `Shift` | B / Circle |
| ნელა სიარული | `Ctrl` (დაჭერით) | მარცხენა shoulder |
| დარტყმა | მაუსის მარცხენა | X / Square |
| ფარის აწევა | მაუსის მარჯვენა (დაჭერით) | მარჯვენა shoulder |
| სრული ეკრანი | `F11` | — |
| მაუსის გათავისუფლება | `Escape` | — |

გაშვებისას პერსონაჟი **ირბენს** — `Ctrl` არის ნელა სიარულის მოდიფიკატორი,
პირიქით არა.

---

## 4. ტესტები

სამივე ტესტი ტერმინალიდან, ფანჯრის გახსნის გარეშე:

```bash
# მოძრაობა, ხტომა, dodge, კიბე, ფერდობი, კამერა, ბალახის რეაქცია
godot --path . --headless --script res://tests/smoke_test.gd

# მონსტრები, ბრძოლა, კიდურების მოჭრა, სისხლი
godot --path . --script res://tests/combat_test.gd -- /tmp

# პოზების სკრინშოტები
godot --path . --script res://tests/pose_shots.gd -- /tmp
```

ყველა შემოწმება `ok` უნდა იყოს; ბოლოში იწერება `All checks passed.`

⚠️ `combat_test`-სა და `pose_shots`-ს **`--headless` არ უნდა მისცე** — ისინი
სკრინშოტებს იღებენ და უფანჯროდ რენდერის დასრულებას ვერასდროს დაელოდებიან
(პროცესი დაეკიდება). `smoke_test` კი პირიქით, უფანჯროდაც სრულად მუშაობს.

---

## 5. სამყაროს ბალახის თავიდან გენერაცია

ბალახი ცალკეულ ტაფტებად კი არ არის მთელ რუკაზე მიმოფანტული, არამედ
**მინდვრებადაა** დაწყობილი — ერთმანეთზე გადამდები ლენტები და ლაქები. ეს
განლაგება გენერირდება სკრიპტით:

```bash
python3 tools/build_scatter.py --dry-run   # მხოლოდ დათვლა, ფაილს არ ცვლის
python3 tools/build_scatter.py             # სცენის თავიდან ჩაწერა
```

მინდვრების ფორმა `tools/build_scatter.py`-ის თავშია — `RIBBONS` (ლენტები),
`BLOBS` (ცალკე ლაქები), `SPACING` (რამდენად ახლოს დგას ბალახი ერთმანეთთან,
რაც ნაკლებია, მით უფრო შეკრულია საფარი) და `NO_GRASS` (სად არ უნდა გაიზარდოს).
თესლი ფიქსირებულია, ანუ ერთი და იგივე პარამეტრები ყოველთვის ერთსა და იმავე
შედეგს იძლევა.

---

## 6. თუ რამე არ მუშაობს

| სიმპტომი | მიზეზი / გამოსავალი |
| -------- | ------------------- |
| `command not found: godot` | იხილე პუნქტი 1 — symlink საჭიროა |
| `Condition "!_project_data_dir..."` ან სცენა არ იტვირთება | არასწორ საქაღალდეშია `--path`; მიუთითე ის, სადაც `project.godot` დევს |
| პირველი გაშვება ნელია | Godot ერთხელ იმპორტს უკეთებს `.glb`/ტექსტურებს `.godot/`-ში; მეორედ სწრაფია |
| ეკრანი შავია, მაუსი გაქრა | მაუსი დაჭერილია თამაშის მიერ — `Escape` |
| FPS დაბალია | იხილე პუნქტი 6 — სამი ღილაკია, რომლითაც სისწრაფე იმართება |

---

## 6. სისწრაფე (FPS)

მთელი დატვირთვა ბალახზე მოდის: `grass2.glb`-ის ერთი ბუჩქი **8 256 სამკუთხედია**
და მათგან ~1700-ია რუკაზე. სამი ღილაკი გვაქვს.

**`Level/Scatter` კვანძზე (ინსპექტორში, Performance ჯგუფი):**

| პარამეტრი | ნაგულისხმევი | რას აკეთებს |
| --------- | ------------ | ----------- |
| `casts_shadows` | `false` | ბალახი ჩრდილს **არ** აგდებს. ჩართვა ~40%-ით ანელებს — თითოეული ბუჩქი ჩრდილის ყოველ კასკადში თავიდან უნდა დაიხატოს |
| `lod_bias` | `0.06` | რამდენად ადრე გადადის ბალახი გამარტივებულ მოდელზე. მცირე რიცხვი = სწრაფი. `1.0` ნიშნავს „LOD-ის გარეშე" და თითქმის ორჯერ ანელებს |
| `draw_distance` | `130` მ | უფრო შორს ბალახი აღარ იხატება. შემცირება ეხმარება, თუ შორ ხედებზე ეცემა |

**`tools/build_scatter.py`-ში (და შემდეგ თავიდან გენერაცია):**

`SPACING` — რამდენად ხშირად დგას ბალახი. რაოდენობა იზრდება `1/SPACING²`-ით,
ანუ `1.05` → `1.25` ბალახს ~30%-ით ამცირებს. ეს არის მთავარი ბერკეტი „უფრო
მკვრივი ბალახი vs უფრო მაღალი FPS"-ს შორის.

გაზომილი შედეგი M1-ზე, ჩრდილოეთ მინდორში დგომისას, მონსტრებთან ბრძოლისას:

| | ადრე | ახლა |
| --- | ---- | ---- |
| 1600×900 | 19 FPS | **62 FPS** |
| 1920×1080 | — | **46 FPS** |
| ჩავარდნები (>25 მწმ კადრი) | კადრების 15% | 0 |
